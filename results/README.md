# Results

Raw CSV/log files are not bundled in this public-safe package. The final validated behavior is summarized in the repository README. If raw experiment logs are later added, remove sensitive/local-only metadata and preserve the original files separately for provenance.
